node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.antigravitycli/
.vercel/
mcp-traces/*-mcp.jsonl
mcp-traces/*-mcp-server.jsonl
.vercel

# Vercel function build artifact — produced by the vercel.json buildCommand
# step that bundles api-src/handler.ts into a self-contained CJS module.
# Vercel deploys api/index.js directly; the source lives in api-src/.
api/index.js
api/index.js.map
